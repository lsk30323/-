# 콜라보레이터 초대 목록 (레포 이전 후 수동 처리)

대상 레포: `goodand/concept-gate-taxonomy` (탁재현)

이 세션의 GitHub 접근은 `lsk30323/-`로 제한되어 있고 MCP에 콜라보레이터
추가 도구가 없어 자동 처리 불가. 레포 등록 후 아래를 수동 초대할 것.

| 대상 | 비고 |
|------|------|
| `goodand` | GitHub 사용자명 — 바로 초대 가능 |
| arraman2969@gmail.com | 이메일 → 해당 계정의 GitHub 사용자명으로 검색 필요 |
| superarraman@naver.com | 이메일 → 해당 계정의 GitHub 사용자명으로 검색 필요 |

방법:
- 웹: Settings → Collaborators → Add people
- CLI: `gh api -X PUT repos/goodand/concept-gate-taxonomy/collaborators/<username>`
